<!DOCTYPE html>
<html>
  <head>
    <title>CampApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript" src="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
    <script src="https://kit.fontawesome.com/8185d53468.js" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.4/jquery.min.js"></script>
  </head>

  <body>
    <div class="header">
      <div><ul class="bar">
        <% if user_signed_in? %>
          <li class="grid-7"><span>おかえりなさい<%= current_user.nickname %>さん</span>
        <% end %>
      </ul> 
      </div>
        <% if user_signed_in? %>      
            <div class="grid-8">
            <div class="g-1">
            <%= link_to "My page", edit_user_registration_path(current_user.id) %>
            </div>
            <div class="g-2">
            <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
            </div>
            <div class="g-3">
            <%= link_to '新規投稿', new_tweet_path(current_user.id)%>
            </div>
          </div>
        <% else%>
          <div class="grid-9">
            <div class="g-4"><%= link_to "Sign In", new_user_session_path, class: "post" %></div>
            <div class="g-5"><%= link_to "Sign Up", new_user_registration_path, class: "post" %></div>
          </div>
        <% end %>
    </div>    
    <%= yield %>
  </body>
</html>
